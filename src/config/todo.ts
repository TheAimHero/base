export const todoTypeArr = ['private', 'public'] as const;

export const todoStatusArr = [
  'active',
  'completed',
  'on hold',
  'deleted',
] as const;

export const todoSortArr = [
  'createdAt',
  'dueDate',
  'title',
  'updatedAt',
] as const;
